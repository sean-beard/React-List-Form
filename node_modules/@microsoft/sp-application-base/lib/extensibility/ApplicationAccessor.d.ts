import { ServiceScope, Event, EventArgs } from '@microsoft/sp-core-library';
/**
 * Provides metadata and events of the SPFx application that's running.
 *
 * @beta
 */
export default class ApplicationAccessor {
    private _navigatedEvent;
    private _privateLayoutChangedEvent;
    constructor(serviceScope: ServiceScope);
    /**
     * Event that gets raised every time there is a page navigation
     */
    readonly navigatedEvent: Event<EventArgs>;
    /**
     * Event that gets raised every time the layout changes in a page
     * @internal
     */
    readonly _layoutChangedEvent: Event<EventArgs>;
}
