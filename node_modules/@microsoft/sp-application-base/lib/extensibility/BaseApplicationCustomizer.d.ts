import { BaseExtension } from '@microsoft/sp-extension-base';
import ApplicationCustomizerContext from './ApplicationCustomizerContext';
/**
 * This is the base class that third parties should extend when implementing
 * a client-side extension that runs when an application is first starting.
 *
 * In the component manifest, the "extensionType" should be set to "ApplicationCustomizer".
 *
 * @beta
 */
declare abstract class BaseApplicationCustomizer<TProperties> extends BaseExtension<TProperties> {
    /**
     * {@inheritdoc @microsoft/sp-extension-base:BaseExtension.context}
     */
    protected readonly context: ApplicationCustomizerContext;
    /**
     * This lifecycle event occurs after the shell has constructed the initial page DOM,
     * after the application's onRender() event has occurred.
     *
     * @deprecated This method is no longer apart of the API. Developers should use
     * the class method {@link @microsoft/sp-extension-base:BaseExtension.onInit | onInit()} to
     * check if the client-side extension is activated on the page.
     *
     */
    onRender(): void;
}
export default BaseApplicationCustomizer;
