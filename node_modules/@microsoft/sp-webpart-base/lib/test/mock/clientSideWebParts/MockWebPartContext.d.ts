import { ServiceScope } from '@microsoft/sp-core-library';
import { HttpClient, SPHttpClient, GraphHttpClient } from '@microsoft/sp-http';
import { IClientSideWebPartManifestInstance } from '@microsoft/sp-module-interfaces';
import IClientSideWebPartStatusRenderer from '../../../core/IClientSideWebPartStatusRenderer';
import { IEventAggregator } from '../../../core/events/IEvent';
import IWebPartHost from '../../../core/IWebPartHost';
import WebPartContext from '../../../core/WebPartContext';
import { IPropertyPaneAccessor } from '../../../core/context/IPropertyPaneAccessor';
import { PropertyPaneState } from '../../../propertyPane/propertyPane/PropertyPaneDefinitions';
export declare class MockWebPartContextParameters {
    domElement: HTMLElement;
    manifest: IClientSideWebPartManifestInstance<{}>;
    instanceId: string;
    webPartTag: string;
    loggingTag: string;
    httpClient: HttpClient;
    spHttpClient: SPHttpClient;
    graphHttpClient: GraphHttpClient;
    parentServiceScope: ServiceScope;
    statusRenderer: IClientSideWebPartStatusRenderer;
    eventAggregator: IEventAggregator;
    host: IWebPartHost;
    propertyPane: IPropertyPaneAccessor;
    isPropertyPaneRenderedByWebPart: () => boolean;
    isPropertyPaneOpen: () => boolean;
    setPropertyPaneState: (id: string, propertyPaneState?: PropertyPaneState, renderedByWebPart?: boolean) => void;
}
export default class MockWebPartContext extends WebPartContext {
    constructor(parameters: MockWebPartContextParameters);
    /**
     * Simulates initialization of the web part, equivalent to what the ClientSideWebPartManager
     * would do.
     */
    initialize<T>(webPart: T): T;
}
