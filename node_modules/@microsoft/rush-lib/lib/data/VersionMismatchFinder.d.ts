import RushConfigurationProject from './RushConfigurationProject';
/**
 * @public
 */
export declare class VersionMismatchFinder {
    private _projects;
    private _mismatches;
    constructor(_projects: RushConfigurationProject[]);
    readonly numberOfMismatches: number;
    getMismatches(): Array<string>;
    getVersionsOfMismatch(mismatch: string): Array<string>;
    getConsumersOfMismatch(mismatch: string, version: string): Array<string>;
    private _analyze();
    private _addDependenciesToList(project, dependencyMap, exclude);
    private _iterableToArray<T>(iterable);
}
