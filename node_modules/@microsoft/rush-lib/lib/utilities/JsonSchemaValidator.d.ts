export declare type ValidateErrorCallback = (errorDescription: string) => void;
/**
 * @alpha
 */
export default class JsonSchemaValidator {
    private _validator;
    private _schemaObject;
    static loadFromFile(schemaFilename: string): JsonSchemaValidator;
    private static _formatErrorDetails(errorDetails, indent, buffer);
    validateObject(jsonObject: Object, errorCallback: ValidateErrorCallback): void;
    private constructor(schemaObject);
}
