import { GraphHttpClient, HttpClient, SPHttpClient } from '@microsoft/sp-http';
import { BaseComponentContext, _IComponentContextParameters } from '@microsoft/sp-component-base';
/**
 * Constructor parameters for ExtensionContext.
 *
 * @internal
 */
export interface IExtensionContextParameters extends _IComponentContextParameters {
}
/**
 * The base class for context objects for client-side extensions.
 *
 * @remarks
 * The constructor for this class is marked as internal; third party code should not inherit
 * directly from this base class.
 *
 * @beta
 */
export default class ExtensionContext extends BaseComponentContext {
    private _graphHttpClient;
    private _httpClient;
    private _spHttpClient;
    /**
     * The constructor for this class is marked as internal; third party code should not inherit
     * directly from this base class.
     *
     * @internal
     */
    constructor(parameters: IExtensionContextParameters);
    /**
     * The instance of GraphHttpClient created for this instance of extension
     */
    readonly graphHttpClient: GraphHttpClient;
    /**
     * The instance of HttpClient created for this instance of extension
     */
    readonly httpClient: HttpClient;
    /**
     * The instance of SpHttpClient created for this instance of extension
     */
    readonly spHttpClient: SPHttpClient;
}
