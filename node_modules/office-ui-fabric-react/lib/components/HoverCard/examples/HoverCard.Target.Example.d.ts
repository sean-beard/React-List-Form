import { BaseComponent } from 'office-ui-fabric-react/lib/Utilities';
import { IColumn } from 'office-ui-fabric-react/lib/DetailsList';
import './HoverCard.Example.scss';
export interface IHoverCardExampleState {
    items?: any[];
    columns?: IColumn[];
}
export declare class HoverCardTargetExample extends BaseComponent<{}, IHoverCardExampleState> {
    constructor(props: {});
    render(): JSX.Element;
    private _onRenderItemColumn(item, index, column);
    private _onRenderCompactCard(item);
    private _onRenderExpandedCard(item);
}
