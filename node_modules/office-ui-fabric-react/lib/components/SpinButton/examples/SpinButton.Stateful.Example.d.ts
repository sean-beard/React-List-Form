import * as React from 'react';
export declare class SpinButtonStatefulExample extends React.Component<any, any> {
    render(): JSX.Element;
    private hasSuffix(string, suffix);
    private removeSuffix(string, suffix);
}
