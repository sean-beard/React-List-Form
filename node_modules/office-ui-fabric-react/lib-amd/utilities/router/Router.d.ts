import { BaseComponent } from '../../Utilities';
export interface IRouterProps {
    /**
     * Gets the component ref.
     */
    componentRef?: () => void;
    replaceState?: boolean;
    children?: any;
    onNewRouteLoaded?: () => void;
}
export declare class Router extends BaseComponent<IRouterProps, {}> {
    componentDidMount(): void;
    render(): JSX.Element;
    private _getPath();
    private _resolveRoute(path?, children?);
}
