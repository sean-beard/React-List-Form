import { BaseComponent } from '../../Utilities';
import { IActivityItemProps } from './ActivityItem.Props';
export interface IActivityItemClassNames {
    root?: string;
    activityContent?: string;
    activityText?: string;
    personaContainer?: string;
    activityPersona?: string;
    activityTypeIcon?: string;
    commentText?: string;
    timeStamp?: string;
}
export declare class ActivityItem extends BaseComponent<IActivityItemProps, {}> {
    private _classNames;
    private _styles;
    constructor(props: IActivityItemProps);
    render(): JSX.Element;
    private _onRenderIcon(props);
    private _onRenderActivityDescription(props);
    private _onRenderComments(props);
    private _onRenderTimeStamp(props);
    private _onRenderPersonaArray(props);
    private _getClassNames(styles, className, activityPersonas, isCompact);
}
