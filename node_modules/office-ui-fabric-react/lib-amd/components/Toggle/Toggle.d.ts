import { BaseComponent } from '../../Utilities';
import { IToggleProps, IToggle } from './Toggle.Props';
export interface IToggleState {
    isChecked: boolean;
}
export declare class Toggle extends BaseComponent<IToggleProps, IToggleState> implements IToggle {
    private _id;
    private _toggleButton;
    constructor(props: IToggleProps);
    /**
     * Gets the current checked state of the toggle.
     */
    readonly checked: boolean;
    componentWillReceiveProps(newProps: IToggleProps): void;
    render(): JSX.Element;
    focus(): void;
    private _onClick(ev);
    private _getClassNames(styles, className, disabled, isChecked);
}
