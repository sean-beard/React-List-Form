import { BaseComponent } from '../../Utilities';
import { IOverflowSet, IOverflowSetProps } from './OverflowSet.Props';
import { FocusZone } from '../../FocusZone';
export declare class OverflowSet extends BaseComponent<IOverflowSetProps, {}> implements IOverflowSet {
    refs: {
        focusZone: FocusZone;
    };
    render(): JSX.Element;
    focus(): void;
    private _onRenderItems(items);
}
