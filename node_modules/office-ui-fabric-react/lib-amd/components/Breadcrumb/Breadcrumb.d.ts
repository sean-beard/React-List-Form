import { BaseComponent } from '../../Utilities';
import { IBreadcrumbProps, IBreadcrumbItem } from './Breadcrumb.Props';
export interface IBreadCrumbData {
    props: IBreadcrumbProps;
    renderedItems: IBreadcrumbItem[];
    renderedOverflowItems: IBreadcrumbItem[];
}
export declare class Breadcrumb extends BaseComponent<IBreadcrumbProps, any> {
    static defaultProps: IBreadcrumbProps;
    private _id;
    constructor(props: IBreadcrumbProps);
    render(): JSX.Element;
    private _onReduceData(data);
    private _onRenderBreadcrumb(data);
    private _onRenderItem(item);
    private _onBreadcrumbClicked(item, ev);
}
